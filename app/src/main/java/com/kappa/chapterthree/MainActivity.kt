package com.kappa.chapterthree

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.util.Log
import android.widget.EditText
import android.widget.TextView

class MainActivity : AppCompatActivity() {
//    lateinit var usernameTextView: TextView
//    lateinit var passwordTextView: TextView


    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

//        usernameTextView = findViewById(R.id.username_text_view)
//        passwordTextView = findViewById(R.id.password_text_view)


//        val usernameResult = intent.getStringExtra("username")
//        val passwordResult = intent.getStringExtra("password")
/**     val name = intent.getStringExtra("name")
        val email = intent.getStringExtra("email")
        val address = intent.getStringExtra("address")
        val age = intent.getIntExtra("age",0)

        val person = intent.getSerializableExtra("object_key") as Person
*/
//        Log.d("MainActivity", usernameResult.toString())
//        Log.d("MainActivity", passwordResult.toString())
//        usernameTextView.text = usernameResult
//        passwordTextView.text = passwordResult

//        val result = intent.getStringExtra("key")
//        Log.d("MainActivity", result.toString())
    }
}